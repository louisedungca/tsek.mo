<nav class="border-2 border-slate-950 w-full h-16 rounded-3xl flex items-center justify-between px-6">
  <h1 class="border-2 border-black border-dotted">
    Welcome, <%= current_user.email %>
  </h1>

  <div class="w-max flex justify-center items-center text-purple-50">
    <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Log out of tsek.mo?" }, class: "actions btn--subv2" %>
  </div>
</nav>