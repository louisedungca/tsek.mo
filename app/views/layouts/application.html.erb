<!DOCTYPE html>
<html>
<head>
  <title>tsek.mo</title>
  <link rel="icon" type="image/x-icon" href="/tsekmo.png">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="relative bg-purple-50 w-full min-h-dvh flex flex-col items-center overflow">
  <%= render partial: "shared/flash_mesgs" %>

  <main class="w-full h-dvh flex items-center justify-center">   
    <section class="border-2 border-pink-500 border-dotted h-full w-full flex flex-col items-center justify-center p-2 overflow-y-auto"> 
      <%= yield %>
    </section>

    <% if params[:controller] == "categories" && params[:action] == "index" %>
      <%= render partial: "task_today" %>
    <% end %>
  </main>
</body>
</html>