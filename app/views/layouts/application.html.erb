<!DOCTYPE html>
<html>
<head>
  <title>tsek.mo</title>
  <link rel="icon" type="image/x-icon" href="/tsekmo.png">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.css" rel="stylesheet" />
  <meta name="viewport" content="width=device-width, initial-scale=1">  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
    
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  
  <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.js" defer></script>
</head>

<body class="relative bg-sky-50 w-full min-h-dvh flex flex-col items-center overflow">
  <%= render partial: "shared/toast_success", locals: { message_success: flash[:notice] } %>
  <%= render partial: "shared/toast_error", locals: {  message_error: flash[:alert] } %>

  <main class="relative w-full h-dvh flex items-center justify-center">   
    <section class="relative h-full w-full flex flex-col items-center justify-center py-2 px-2"> 
      <%= render partial: "shared/nav" %>
      <div class="relative h-full w-full flex flex-col justify-center items-center gap-2 px-4 overflow">
        <%= yield %>
      </div>      
    </section>

    <% if params[:controller] == "categories" && params[:action] == "index" %>
      <%= render partial: "task_today" %>
    <% end %>
  </main>
</body>
</html>